extends layout

block content
  h1= title
  p Welcome to #{title}
  p Welcome again.
  button(type="button", class="btn btn-info" onclick="doReduce()") Press Me
  div#reduce Reduce the value
  a(href="racecar") Racecar

  script.
    let count = 20;
    let change = -1;
    function doReduce() {
      count += change;
      change -= 1;
      document.getElementById('reduce').textContent = `Count ${count} with change ${change}.`;
    }
